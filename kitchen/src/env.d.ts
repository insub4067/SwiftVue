// The stylesheet has no types and needs none — this only tells TypeScript
// the import is deliberate. It mirrors `import 'swiftvue/styles'`, which a
// real consumer writes and which resolves through the package's export map.
declare module '@swiftvue/styles'
